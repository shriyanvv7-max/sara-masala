"use client";
import { useState } from "react"; import { StoreNav } from "../../components/store";
export default function Contact(){const [done,setDone]=useState(false);const [pending,setPending]=useState(false);const [message,setMessage]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();if(pending)return;setPending(true);setMessage("");const form=new FormData(event.currentTarget);try{const response=await fetch("/api/contact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name:form.get("name"),email:form.get("email"),message:form.get("message"),website:form.get("website")||""})});const json=await response.json();if(!response.ok)throw new Error(json.error);setDone(true)}catch(error){setMessage(error instanceof Error?error.message:"We could not send your message. Please try again.")}finally{setPending(false)}}return <><StoreNav/><main className="contact-page"><p className="eyebrow">LET'S TALK MASALA</p><h1>We'd love to<br/><em>hear from you.</em></h1><div className="contact-grid"><form onSubmit={submit}>{done?<p>Thank you. We will be in touch soon.</p>:<><input required name="name" maxLength={120} autoComplete="name" placeholder="Your name"/><input required name="email" maxLength={254} autoComplete="email" type="email" placeholder="Email address"/><textarea required name="message" minLength={10} maxLength={5000} placeholder="How can we help?"/><input name="website" tabIndex={-1} autoComplete="off" aria-hidden="true" style={{position:"absolute",left:"-10000px"}}/><button type="submit" className="add-cart" disabled={pending}>{pending?"Sending…":"Send message"}</button>{message&&<p role="alert">{message}</p>}</>}</form><aside><h3>Visit us</h3><p>Mysuru, Karnataka<br/>India</p><h3>Contact</h3><p>hello@saramasala.in<br/>+91 98765 43210</p><h3>Instagram</h3><p><a href="https://www.instagram.com/sara.masala_co/?hl=en" target="_blank" rel="noreferrer">@sara.masala_co</a></p><div className="map">MYSURU<br/>MAP PLACEHOLDER</div></aside></div></main></>}
