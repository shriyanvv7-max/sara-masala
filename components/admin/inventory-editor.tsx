"use client";
import { useState } from "react";
type Variant={id:string;weight:string;price:number;stock:number;sku:string;products?:{name:string}|null};
export function InventoryEditor({initial}:{initial:Variant[]}){const [rows,setRows]=useState(initial);const [saved,setSaved]=useState("");const change=(id:string,key:"price"|"stock"|"sku",value:string)=>setRows(rows=>rows.map(row=>row.id===id?{...row,[key]:key==="sku"?value:Number(value)}:row));async function save(row:Variant){const r=await fetch(`/api/product-variants/${row.id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({...row,active:true})});if(!r.ok)throw new Error();}async function saveAll(){setSaved("");try{await Promise.all(rows.map(save));setSaved("Inventory saved.")}catch{setSaved("Unable to save inventory. Please try again.")}}return <><div className="inventory-actions"><button className="add-cart" onClick={saveAll}>Bulk save</button>{saved&&<span>{saved}</span>}</div><div className="admin-table inventory-table">{rows.map(row=><article key={row.id}><div><b>{row.products?.name}</b><small>{row.weight}</small></div><input type="number" value={row.price} onChange={e=>change(row.id,"price",e.target.value)}/><input type="number" value={row.stock} onChange={e=>change(row.id,"stock",e.target.value)}/><input value={row.sku} onChange={e=>change(row.id,"sku",e.target.value)}/><button onClick={()=>save(row).then(()=>setSaved("Variant saved.")).catch(()=>setSaved("Unable to save variant."))}>Save</button></article>)}</div></>}
